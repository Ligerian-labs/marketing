node_modules/
dist/
.astro/
.DS_Store
.env
.env.*

# Anchored to the repo root: an unanchored `data/` also matched `src/data/`,
# which silently kept source files out of the repo.
/leads/*.json
/leads/*.jsonl
/data/
