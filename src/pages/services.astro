---
// The old /services route is now /offres. Kept as a permanent redirect so the
// existing inbound links and indexed URLs don't break.
export const prerender = false;

return Astro.redirect("/offres", 301);
---
