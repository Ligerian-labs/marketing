---
interface Props {
  href?: string;
  class?: string;
}

const { href = "/", class: className = "" } = Astro.props;
---

<a href={href} class:list={["ll-logo", className]} aria-label="Ligerian Labs — accueil">
  Ligerian <span>Labs</span>
</a>
